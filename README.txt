Group 8 
Ellison Beard	Abbey Martin	Justin Pettyjohn	Nghia Le

Should there be any issues establishing a connection to the database or other connection errors, We would like to demo the system.

Our test file creates, stores and deletes intsance objects of classes created in out system.

Open MySQL database, connect to Database
Open text editor and open eVote.
in File DbAccessConfig.java change all instances of the connection string to local CPU connection string.

Execute the SQL scripts located in the SQL folder of the project files
Run the Tester on project code, the Tester file is located in the outer-most eVote file

